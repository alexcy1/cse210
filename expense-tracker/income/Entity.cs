using System;

// Definition of the Entity class
class Entity
{
    // Private field to store the ID
    private int _id;

    // Getter method to retrieve the ID
    public int GetId() => _id;

    // Setter method to set the ID
    public void SetId(int id) => _id = id;
}
